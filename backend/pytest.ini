[pytest]
pythonpath = .
testpaths = tests
markers =
    integration: marks tests as integration tests that may require external services (deselect with '-m "not integration"')
    mongodb: marks tests as requiring a MongoDB connection
